version: '2'
services:
  memes-finder-api:
    build: .
    environment:
      - MONGO_URL=mongodb://mongodb:27017/memesfinder
    volumes:
      - .:/app
    ports:
      - '3000:3000'
    depends_on:
      - mongodb

  mongodb:
    image: mongo
    ports:
      - '27017:27017'
